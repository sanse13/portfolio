import { TextTransitionDirective } from './text-transition.directive';

describe('TextTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
